---
galaxy_info:
  author: Daniel von Essen
  description: This role configures systemd-timesyncd (man 5 systemd-timesyncd)
  license: MIT

  min_ansible_version: 2.9

  platforms:
    - name: EL
      versions:
        - 8
    - name: Debian
      versions:
        - buster
    - name: Ubuntu
      versions:
        - focal
    - name: ArchLinux
      versions:
        - Any

  galaxy_tags: [ntp, chrony, system, timesync, network, time]

dependencies:
  - src: git+https://gitlab.com/dvonessen/ansible-role-epel
    version: master
    name: epel
